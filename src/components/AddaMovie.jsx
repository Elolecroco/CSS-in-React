import { useEffect } from "react"

function AddaMovie() {
    const onClick = () => {
        alert('Joking, still haven\'t coded this')
    }
    return (
        <button onClick={onClick}>Add a Movie</button>
    )
}

export default AddaMovie